<lifecycle:BasePage
    x:Class="StateByState.ThirdPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StateByState"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lifecycle="using:BuiltToRoam.Lifecycle"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <local:CustomSplitView DisplayMode="Inline" IsPaneOpen="True" OpenPaneLength="400" Background="Green" x:Name="Split">
            <local:CustomSplitView.Pane>
                <StackPanel>
                    <Button Click="LoadOneClick">Option One</Button>
                    <Button Click="LoadTwoClick">Option Two</Button>
                    <Button Click="LoadThreeClick">Option Three</Button>
                    <Button Click="LoadFourClick">Option Four</Button>
                </StackPanel>
            </local:CustomSplitView.Pane>
            <local:CustomSplitView.Content>
                <Frame x:Name="InnerFrame" />
            </local:CustomSplitView.Content>
            
        </local:CustomSplitView>
    </Grid>
</lifecycle:BasePage>
